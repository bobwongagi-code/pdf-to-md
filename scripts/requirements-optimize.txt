# File Optimization Dependencies
# Install with: pip install -r scripts/requirements-optimize.txt

# Image processing; pinned to the version covered by the local image tests
Pillow==10.4.0

# PDF processing
pypdfium2==4.30.0
